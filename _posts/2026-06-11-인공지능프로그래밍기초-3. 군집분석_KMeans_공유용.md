---
layout: post
title:  "[트리, 비지도학습] 인공지능프로그래밍기초 - 3. 군집분석 KMeans"
date:   2026-06-11 12:07:00 +0900
categories: 인공지능프로그래밍기초
---

# 군집분석 KMeans 공유용 정리

## 1) Google Colab 데이터 로드

```python
# Step 1. 분석할 데이터가 저장된 파일을 불러와서 변수에 할당합니다.
from google.colab import files
myfile = files.upload()

import io
import pandas as pd

# pd.read_csv로 csv파일 불러오기
fruits = pd.read_csv(io.BytesIO(myfile['과일3개.csv']), encoding='cp949')
fruits
```

## 2) 로컬 데이터 로드

```python
# 컴퓨터에서 작업하려면 아래 코드를 실행하면 됩니다.
import pandas as pd
fruits = pd.read_csv('./머신러닝실습용자료/과일3개.csv', encoding='cp949')
fruits
```

주의: 로컬에서 FileNotFoundError가 나면 `./머신러닝실습용자료/과일3개.csv` 파일이 현재 폴더 기준으로 존재하는지 먼저 확인하세요.

## 3) 공통 실습 코드

### Step 2. 데이터 분포와 초기 중심점 시각화

```python
import matplotlib.pyplot as plt

x1, y1 = 2000, 22
x2, y2 = 200, 2.5
x3, y3 = 500, 10

data = fruits[['무게_g', '길이_cm']]

plt.figure(figsize=(7, 5))
plt.title('Before', fontsize=15)
plt.plot(data['무게_g'], data['길이_cm'], 'o', label='Data')
plt.plot([x1, x2, x3], [y1, y2, y3], 'rD', marker='*', markersize=12, label='init_Centroid')
plt.xlabel('Weight', fontsize=12)
plt.ylabel('Length', fontsize=12)
plt.legend()
plt.grid()
plt.show()
```

![초기 중심점 시각화](/assets/img/kmeans_before.png)

### Step 3. KMeans 군집 분석 수행

```python
from sklearn.cluster import KMeans
import numpy as np

# 군집 분석은 비지도학습이기에 target이 없습니다.
data = fruits[['무게_g', '길이_cm']]

# 초기 중심점을 지정할 경우
kmeans = KMeans(n_clusters=3, init=np.array([(x1, y1), (x2, y2), (x3, y3)]))

# 초기 중심점을 지정하지 않을 경우
# kmeans = KMeans(n_clusters=3)

# 모델 학습
kmeans.fit(data)

# 라벨과 중심점 좌표 확인
data['cluster'] = kmeans.labels_
final_centroid = kmeans.cluster_centers_
```

```python
data
```

### Step 4. 최종 군집 결과 시각화

```python
plt.figure(figsize=(7, 5))
plt.title('After', fontsize=15)
plt.scatter(data['무게_g'], data['길이_cm'], c=data['cluster'])
plt.plot(final_centroid[:, 0], final_centroid[:, 1], 'rD', marker='*', markersize=12, label='final_Centroid')
plt.xlabel('Weight', fontsize=12)
plt.ylabel('Length', fontsize=12)
plt.legend()
plt.grid()
plt.show()
```

![최종 군집 결과](/assets/img/kmeans_after.png)

### 추가 확인 코드

```python
# 각각의 요소에 대한 라벨 출력
print(kmeans.labels_)
```

```python
# [500, 20] 데이터 넣었을 때 예측값 확인
kmeans.predict([[500, 20]])
```

```python
# [1700, 15] 데이터 넣었을 때 예측값 확인
kmeans.predict([[1700, 15]])
```

```python
# 클러스터 중심과 클러스터에 속한 샘플 사이 거리 제곱합
print(kmeans.inertia_)
```

### 최적 군집 개수 찾기 (Elbow Method)

```python
import matplotlib.pyplot as plt

inertia = []
for i in range(2, 15):
    km = KMeans(n_clusters=i)
    km.fit(data)
    inertia.append(km.inertia_)

plt.plot(range(2, 15), inertia)
plt.show()
```

![엘보우 결과](/assets/img/kmeans_elbow.png)