---
layout: post
title: "SQL Python으로 연동하기"
date: 2026-05-18 11:00:00 +0900
categories: [Programming, Python, DataAnalysis]
---

# 데이터 정제하기

이 문서는 `데이터정제하기.ipynb`의 내용을 Markdown 형식으로 정리한 것이다.

## 1. 실습 환경 설정하기

```python
import pymysql
import pandas as pd

conn = pymysql.connect(
    host='localhost',
    user='taesub',
    password='0204',
    database='processsensordb',
    charset='utf8mb4'
)
```

원시 데이터를 조회한다.

```python
query = """
select 
*
from sensormeasurement sm 
join sensor s 
on sm.sensor_id = s.sensor_id;
"""

df = pd.read_sql(query, conn)
print(df.head())
```

공정 실행 이력 데이터도 함께 조회한다.

```python
query = """
select 
    RH.run_id,
    E.equipment_id,
    E.model_name,
    PS.step_name,
    PS.process_group,
    RH.run_status,
    RH.start_time,
    RH.lot_id
from RunHistory as RH
join Equipment as E on RH.equipment_id = E.equipment_id
join ProcessStep as PS on PS.step_id = RH.step_id
"""

df = pd.read_sql(query, conn)
print(df.head())
```

## 2. 필요한 데이터 선택하기

```python
df = df[['run_id', 'sensor_name', 'measured_value']]
print(df.head())
```

특정 센서만 선택한다.

```python
temp_df = df[df['sensor_name'] == 'chamber_temp']
print(temp_df)
```

조건 필터링 예시.

```python
high_temp_df = df[df['measured_value'] >= 410]
print(high_temp_df)
```

## 3. 데이터 묶기: groupby

센서별 평균 측정값을 계산한다.

```python
sensor_mean = df.groupby('sensor_name')['measured_value'].mean()
print(sensor_mean)
```

장비별 실행 횟수를 계산한다.

```python
runcount_per_equip = df.groupby('equipment_id')['run_id'].count()
print(runcount_per_equip)
```

## 4. 데이터 형태 변환하기: pivot_table

run_id 기준으로 센서별 측정값을 펼친다.

```python
pivot_df = df.pivot_table(
    index = 'run_id',
    columns = 'sensor_name',
    values = 'measured_value'
)

pivot_df
```

최대값 기준으로 집계할 수도 있다.

```python
df.pivot_table(
    index = 'run_id',
    columns='sensor_name',
    values='measured_value',
    aggfunc='max'
)
```

개수 집계 예시.

```python
df.pivot_table(
    index = 'run_id',
    columns='sensor_name',
    values='measured_value',
    aggfunc='count'
)
```

분석용 데이터셋을 CSV로 저장한다.

```python
pivot_df.to_csv(
    'clean_sensor_data.csv',
    encoding = 'utf-8-sig'
)
```

## 5. 데이터 정제 실습 문제

### 문제 1
RunHistory, Equipment, ProcessStep 테이블을 JOIN하여 공정 실행 이력 데이터를 조회한다.

```python
query = """
select 
    r.run_id,
    e.equipment_id,
    e.model_name,
    p.step_name,
    p.process_group,
    r.run_status,
    r.start_time,
    r.lot_id
from runhistory as r
join equipment as e 
on r.equipment_id = e.equipment_id
join processstep as p 
on r.step_id = p.step_id;
"""

df = pd.read_sql(query, conn)
print(df.head())
```

### 문제 2
`run_id`, `step_name`, `run_status`만 선택한다.

```python
print(df[['run_id', 'step_name', 'run_status']])
```

### 문제 3
`equipment_id`, `model_name`, `process_group`만 선택한다.

```python
temp_df = df[['equipment_id', 'model_name', 'process_group']]
temp_df
```

### 문제 4
`run_status`가 `warning`인 데이터만 추출한다.

```python
newdf = df[df['run_status'] == 'warning']
newdf
```

### 문제 5
`process_group`이 `Etch`인 데이터만 조회한다.

```python
temp_df = df[df['process_group'] == 'Etch']
print(temp_df)
```

### 문제 6
공정 그룹별 공정 실행 횟수를 계산한다.

```python
newdf = df.groupby('process_group')['run_id'].count()
newdf
```

### 문제 7
장비별 수행 공정 수를 계산한다.

```python
newdf1 = df.groupby(['equipment_id', 'model_name'])['run_id'].count()
newdf1 = newdf1.reset_index()
print(newdf1)
print(type(newdf1))

newdf1.columns = ['equipment_id', 'model_name', 'run_count']
print(newdf1)
```

### 문제 8
`equipment_id`와 `process_group` 기준으로 pivot_table을 생성한다.

```python
pivot_df = df.pivot_table(
    index = 'equipment_id',
    columns = 'process_group',
    values = 'run_id',
    aggfunc = 'count'
)

pivot_df
```

### 문제 9
`lot_id`와 `run_status` 기준으로 pivot_table을 생성한다.

```python
pivot_df = df.pivot_table(
    index = 'lot_id',
    columns = 'run_status',
    values = 'run_id',
    aggfunc = 'count'
)

pivot_df
```

### 문제 10
공정 실행 분석용 데이터셋을 만들고 CSV로 저장한다.

```python
pivot_df = pivot_df.reset_index()

pivot_df.to_csv(
    'temp_df.csv',
    index = False,
    encoding = 'utf-8-sig'
)
```

실제로는 파일 이름을 `equipment_process_summary.csv`로 저장하는 것이 문제 조건에 더 정확하다.

```python
pivot_df.to_csv(
    'equipment_process_summary.csv',
    index = False,
    encoding = 'utf-8-sig'
)
```
